import React from "react";

type Props = {};

const ProfileTabs = (props: Props) => {
  return (
    <div>
      {/* lets do filter option later */}
      {/* search also later */}
    </div>
  );
};

export default ProfileTabs;
